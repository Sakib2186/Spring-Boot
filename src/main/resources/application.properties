spring.application.name=jobportal
logging.pattern.console="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"


#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
#spring.datasource.url=jdbc:h2:mem:testdb for in memory H2 DB which won't be stored

#.datasource.url=jdbc:h2:file:D:/JAVA PROJECTS/SPRING/PROJECTS/JOB_PORTAL/jobportal/src/main/resources/H2database;AUTO_SERVER=true
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver

# spring.sql.init.mode=embedded onyl works for in memoery DB
#spring.sql.init.mode=always  change to embedded or comment when using file H2 DB to prevent data being loaded again and again
#spring.sql.init.schema-locations=optional:classpath:sql/jobportal-schema.sql
#spring.sql.init.data-locations=optional:classpath:sql/jobportal-data.sql

#spring.jpa.show-sql=${SHOW_SQL:true}

# MYSQL setup

spring.datasource.url= jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:jobportal}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=${HIBERANTATE_SQL_QUERY:true}


#spring.docker.compose.file =
#spring.docker.compose.lifecyle-management=
#spring.docker.compose.start.command=up (will pull, create container)
#spring.docker.compose.start.commad=start (will start paused containers wont pull)
#spring.docker.compose.stop.command=down (will delete container)